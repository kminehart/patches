From 5cd3dd4095a8c83203a2cb2d8c8c502a1fdcd8cb Mon Sep 17 00:00:00 2001
From: Kevin Minehart <kmineh0151@gmail.com>
Date: Thu, 15 Jun 2023 15:12:11 -0500
Subject: [PATCH 1/2] Add Sub function and test

---
 main.go      | 5 +++++
 main_test.go | 6 ++++++
 2 files changed, 11 insertions(+)

diff --git a/main.go b/main.go
index c34396b..451dad2 100644
--- a/main.go
+++ b/main.go
@@ -6,6 +6,11 @@ func Add(a, b int) int {
 	return a + b
 }
 
+func Sub(a, b int) int {
+	return a - b
+}
+
 func main() {
 	log.Println(Add(1, 2))
+	log.Println(Sub(2, 1))
 }
diff --git a/main_test.go b/main_test.go
index fc0509f..33c8e8b 100644
--- a/main_test.go
+++ b/main_test.go
@@ -10,3 +10,9 @@ func TestAdd(t *testing.T) {
 		t.Fatalf("Add(2, 2) should equal '%d' but equals '%d'", 3, res)
 	}
 }
+
+func TestSub(t *testing.T) {
+	if res := Sub(2, 1); res != 1 {
+		t.Fatalf("Sub(2, 1) should equal '%d' but equals '%d'", 1, res)
+	}
+}
-- 
2.40.1


From df5aec522d3fdf916acc7ee43cf50aeb94327b9a Mon Sep 17 00:00:00 2001
From: Kevin Minehart <kmineh0151@gmail.com>
Date: Fri, 16 Jun 2023 08:21:34 -0500
Subject: [PATCH 2/2] add new-file.txt and update the github workflow

---
 .github/workflows/sync-patch.yml | 8 +++++---
 new-file.txt                     | 2 ++
 2 files changed, 7 insertions(+), 3 deletions(-)

diff --git a/.github/workflows/sync-patch.yml b/.github/workflows/sync-patch.yml
index 55ad4f9..af7db80 100644
--- a/.github/workflows/sync-patch.yml
+++ b/.github/workflows/sync-patch.yml
@@ -1,8 +1,7 @@
 name: Sync pull requests to the patches repository
 
 on:
-  pull_request:
-    types: [opened, reopened, synchronized]
+  pull_request: {}
   workflow_dispatch:
 # A workflow run is made up of one or more jobs that can run sequentially or in parallel
 jobs:
@@ -13,6 +12,8 @@ jobs:
     # Steps represent a sequence of tasks that will be executed as part of the job
     steps:
       - uses: actions/checkout@v3
+        with:
+          ref: ${{ github.event.pull_request.head.sha }}
       - name: Checkout active patches
         uses: actions/checkout@v3
         with:
@@ -20,5 +21,6 @@ jobs:
           path: patches
       - name: Create patch
         run: |
-          git format-patch ${{github.event.pull_request.head}}..${{github.event.pull_request.head}} --stdout > diff.patch
+          git fetch --unshallow origin ${{github.event.pull_request.base.ref}}
+          git format-patch origin/${{github.event.pull_request.base.ref}} --stdout > diff.patch
           cat diff.patch
diff --git a/new-file.txt b/new-file.txt
index a453e43..cfd1f3f 100644
--- a/new-file.txt
+++ b/new-file.txt
@@ -1 +1,3 @@
 idk
+
+-- security fix again --
-- 
2.40.1
